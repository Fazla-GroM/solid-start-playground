diff --git a/node_modules/solid-start/vite/plugin.js b/node_modules/solid-start/vite/plugin.js
index f04eb03..16462b8 100644
--- a/node_modules/solid-start/vite/plugin.js
+++ b/node_modules/solid-start/vite/plugin.js
@@ -543,7 +543,9 @@ function find(locate, cwd) {
   cwd = cwd || process.cwd();
   if (cwd.split(path.sep).length < 2) return undefined;
   const match = fs.readdirSync(cwd).find(f => f === locate);
-  if (match) return match;
+ 
+  if (match) return path.join(cwd, match);
+
   return find(locate, path.join(cwd, ".."));
 }
 
